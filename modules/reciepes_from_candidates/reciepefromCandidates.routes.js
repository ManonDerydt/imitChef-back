const router = require("express").Router();
const reciepeFromCandidates = require("./reciepefromCandidates.middlewares");

// router.route("/reciepe-from-candidates").get(reciepeFromCandidates.getRecipeFromCandidates);

module.exports = router;